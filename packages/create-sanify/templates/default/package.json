{
  "name": "__PROJECT_NAME__",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev-server.ts",
    "build": "rm -rf dist && bun build ./index.html --outdir ./dist --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sanify/core": "^0.5.4"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
